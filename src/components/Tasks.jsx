import { Typography } from '@mui/material'
import React from 'react'
import { CustomBox } from './'

const Tasks = () => {
    return (
        <CustomBox>
            <Typography variant='body1' color="white" textAlign="center">No hay Tareas</Typography>
        </CustomBox>
    )
}

export default Tasks